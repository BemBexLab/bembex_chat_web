services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL}
    env_file:
      - .env
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
